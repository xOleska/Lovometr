Windows:
   1. python -m pip install wheel setuptools pip --upgrade
   2. pip install Pillow
   3. pip install pywin32
   4. pip install pygame
   5. python lovemeter.py

MacOS/Linux:
   1. python -m pip install wheel setuptools pip --upgrade
   2. pip install Pillow
   3. pip install pygame
   4. python lovemeter_unix.py